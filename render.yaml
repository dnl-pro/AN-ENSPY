services:
  - type: web
    name: rag-an-enspy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app/main.py
    envVars:
      - key: GROQ_API_KEY
        sync: false